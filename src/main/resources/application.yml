spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://121.139.79.201:30001:3306/cafe_review?chracterEncoding=UTF-8&serverTimezone=UTC
    username: dhs_msa
    password: dhs_msa

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDB103Dialect
    show-sql: true

  h2:
    console:
      enabled: true

# ?? ???? ????? ??
logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace
